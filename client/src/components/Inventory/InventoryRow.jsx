import { Pencil, Trash2 } from "lucide-react";
const colors={safe:"bg-emerald-100 text-emerald-800",near_expiry:"bg-amber-100 text-amber-800",today:"bg-orange-100 text-orange-800",expired:"bg-red-100 text-red-800"};
function InventoryRow({item,onEdit,onDelete}) { const expiry=item.expiry || {}; return <tr className="border-b border-slate-100 hover:bg-slate-50"><td className="px-4 py-3"><p className="font-semibold">{item.product_name}</p><p className="text-xs text-slate-500">ID: {item.product_code || item.product_id}{item.barcode ? ` · ${item.barcode}` : ""}</p></td><td className="px-4 py-3">{item.category}</td><td className="px-4 py-3">{item.quantity} {item.unit}</td><td className="px-4 py-3">{item.supplier || "—"}<br/>{item.batch_number && <span className="text-xs text-slate-500">Batch {item.batch_number}</span>}</td><td className="px-4 py-3">{new Date(item.expiry_date).toLocaleDateString()}</td><td className="px-4 py-3"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${colors[expiry.level] || "bg-slate-100"}`}>{expiry.label || "Safe"}{expiry.daysRemaining !== null && expiry.daysRemaining !== undefined ? ` · ${expiry.daysRemaining}d` : ""}</span></td><td className="px-4 py-3"><div className="flex gap-3"><button title="Edit" onClick={()=>onEdit(item)} className="text-blue-700"><Pencil size={18}/></button><button title="Delete" onClick={()=>onDelete(item.inventory_id)} className="text-red-700"><Trash2 size={18}/></button></div></td></tr>; }
export default InventoryRow;
