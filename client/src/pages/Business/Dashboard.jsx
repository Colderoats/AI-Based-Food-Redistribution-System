import { useEffect, useMemo, useState } from "react";
import { BellRing, BrainCircuit, HeartHandshake, Package, Truck } from "lucide-react";
import DashboardLayout from "../../layouts/DashboardLayout";
import { getExpiryAlerts, getInventory } from "../../services/inventoryService";

function Dashboard(){const [inventory,setInventory]=useState([]),[alerts,setAlerts]=useState([]);const load=async()=>{try{const [items,expiry]=await Promise.all([getInventory(),getExpiryAlerts()]);setInventory(items.inventory||[]);setAlerts(expiry.alerts||[]);}catch{setInventory([]);setAlerts([]);}};useEffect(()=>{const timer=setTimeout(load,0);return()=>clearTimeout(timer);},[]);const stats=useMemo(()=>({near:inventory.filter((item)=>item.expiry?.level==="near_expiry"||item.expiry?.level==="today").length,expired:inventory.filter((item)=>item.expiry?.level==="expired").length}),[inventory]);const cards=[["Total products",inventory.length,Package,"text-emerald-300"],["Near expiry",stats.near,BellRing,"text-amber-300"],["Expired",stats.expired,Package,"text-red-300"],["Active alerts",alerts.length,BellRing,"text-cyan-300"]];return <DashboardLayout title="Dashboard" role="Food Business"><div className="space-y-6"><div><h1 className="text-3xl font-bold">Business dashboard</h1><p className="mt-1 text-slate-400">Your inventory health and operational priorities at a glance.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,Icon,color])=><div key={label} className="rounded-xl border border-slate-800 bg-slate-900 p-5"><Icon className={color} size={21}/><p className="mt-5 text-sm text-slate-400">{label}</p><p className="mt-1 text-3xl font-bold">{value}</p></div>)}</div><div className="grid gap-6 xl:grid-cols-2"><section className="rounded-xl border border-slate-800 bg-slate-900 p-5"><h2 className="font-semibold">Expiry alerts</h2>{alerts.length?<div className="mt-4 space-y-3">{alerts.slice(0,5).map((item)=><div key={item.inventory_id} className="flex items-center justify-between border-b border-slate-800 pb-3 text-sm"><div><p className="font-medium">{item.product_name}</p><p className="text-slate-400">{item.category} · expires {new Date(item.expiry_date).toLocaleDateString()}</p></div><span className="text-amber-300">{item.expiry?.label}</span></div>)}</div>:<p className="mt-4 text-sm text-slate-400">No active expiry alerts. Your inventory is in a healthy state.</p>}</section><section className="rounded-xl border border-slate-800 bg-slate-900 p-5"><h2 className="font-semibold">Recent inventory additions</h2>{inventory.length?<div className="mt-4 space-y-3">{inventory.slice(0,5).map((item)=><div key={item.inventory_id} className="flex justify-between border-b border-slate-800 pb-3 text-sm"><span>{item.product_name}</span><span className="text-slate-400">{item.quantity} {item.unit}</span></div>)}</div>:<p className="mt-4 text-sm text-slate-400">Recent additions will appear after you add inventory.</p>}</section></div><div className="grid gap-5 md:grid-cols-3"><Placeholder icon={BrainCircuit} title="Waste risk (AI)" text="Reserved for waste-risk prediction and recommended actions."/><Placeholder icon={HeartHandshake} title="Donation opportunities" text="Reserved for matching eligible surplus with local NGOs."/><Placeholder icon={Truck} title="NGO communication" text="NGO contacts, requirements, deadlines and logistics will appear here."/></div></div></DashboardLayout>;}
function Placeholder({icon:Icon,title,text}){return <section className="rounded-xl border border-dashed border-slate-700 bg-slate-900/60 p-5"><Icon className="text-slate-400" size={21}/><h2 className="mt-4 font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{text}</p></section>;}
export default Dashboard;
