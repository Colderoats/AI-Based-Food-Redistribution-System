import DashboardLayout from "../../layouts/DashboardLayout";

function Overview() { const user = JSON.parse(localStorage.getItem("user") || "{}"); const isBusiness = user.role === "business"; return <DashboardLayout title={isBusiness ? "Business overview" : "NGO overview"} role={isBusiness ? "Food Business" : "Food Recipient Organization"}><section className="max-w-3xl rounded-lg border border-slate-800 bg-slate-900 p-7"><h2 className="text-xl font-semibold">{user.name || "Your organization"}</h2><p className="mt-3 leading-7 text-slate-400">{isBusiness ? "Manage food inventory, publish surplus, coordinate pickups, and connect with nearby NGOs from this workspace." : "Find nearby food businesses, request available surplus, coordinate pickups, and track the impact of every redistribution."}</p><div className="mt-6 rounded border border-dashed border-cyan-500/30 bg-cyan-500/5 p-5"><h3 className="font-semibold text-cyan-200">AI matching workspace</h3><p className="mt-2 text-sm text-slate-400">Reserved for expiry-risk predictions, food demand matching, and pickup recommendations once the model service is connected.</p></div></section></DashboardLayout>; }
export default Overview;
